# requirements.txt (Minimal Logical Dependencies)
# NOTE: Installing this will still pull in many transitive dependencies,
# including potentially chromadb via crewai-tools.
crewai>=0.114.0
crewai-tools>=0.42.0
python-dotenv>=1.0.0
openai>=1.76.0
langchain-core>=0.2.30